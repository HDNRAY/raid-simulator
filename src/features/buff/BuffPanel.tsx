const BuffPanel = (props: {
    className?: string
}) => {
    const { className } = props;
    return <div className={`${className}`}>
        <div></div>
        <div>

        </div>
    </div>
}

export default BuffPanel;